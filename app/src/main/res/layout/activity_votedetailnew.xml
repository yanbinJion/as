<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/realcontent"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tab"
        android:layout_alignParentTop="true"
        android:layout_below="@+id/tab" />


    <com.flyco.tablayout.CommonTabLayout
        android:id="@+id/tab"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dimen_50"
        android:layout_alignParentBottom="true"
        android:background="@color/bg_gray"
        app:tl_indicator_color="@color/gray_line"
        app:tl_indicator_corner_radius="@dimen/dimen_0"
        app:tl_indicator_style="BLOCK"
        app:tl_iconMargin="@dimen/dimen_2"
        app:tl_indicator_margin_bottom="@dimen/dimen_0"
        app:tl_indicator_margin_top="@dimen/dimen_0"
        app:tl_textSelectColor="@color/text_gray"
        app:tl_textUnselectColor="@color/text_gray" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_above="@+id/tab"
        android:background="@color/life_bg" />

</RelativeLayout>